openapi: 3.1.0
info:
  title: eventbridge
paths:
  /:
    POST:
      summary: Update Endpoint
      description: >-
        Revise the existing endpoint in my API. Learn more about global
        endpoints and enhancing regional fault tolerance through event
        replication in the Amazon EventBridge User Guide.
      tags:
        - Update
        - Endpoints
