openapi: 3.1.0
info:
  title: codebuild
paths:
  /:
    POST:
      summary: 'Update Webhook '
      description: >-
        This API allows you to update the webhook linked to a CodeBuild build
        project. If your repository is hosted on Bitbucket, the rotateSecret
        parameter will be disregarded.
      tags:
        - Webhooks
